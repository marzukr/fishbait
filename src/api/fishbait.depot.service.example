[Unit]
Description=Fishbait Game State Depot
Requires=network.target
After=network.target

[Service]
User=ubuntu
WorkingDirectory=PLACEHOLDER
ExecStart=PLACEHOLDER depot.py
Restart=always

[Install]
WantedBy=multi-user.target
